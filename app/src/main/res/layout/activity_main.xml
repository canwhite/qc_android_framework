<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/containner"
        android:layout_weight="1"
        tools:ignore="Suspicious0dp"
        android:background="#f5f5f5"
        >
    </FrameLayout>


    <RadioGroup
        android:id="@+id/rg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/radioButton_home"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/bottom_icon_img_selector_home"
            android:gravity="center"
            android:text="首页"
            android:textColor="@drawable/bottom_icon_text_selector"
            android:checked="true"/>
        <RadioButton
            android:id="@+id/radioButton_service"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/bottom_icon_img_selector_service"
            android:gravity="center"
            android:text="服务"
            android:textColor="@drawable/bottom_icon_text_selector" />
        <RadioButton
            android:id="@+id/radioButton_mine"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/bottom_icon_img_selector_mine"
            android:gravity="center"
            android:text="个人"
            android:textColor="@drawable/bottom_icon_text_selector" />

    </RadioGroup>


</LinearLayout>